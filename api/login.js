const{makeSession,json,body}=require('./_lib');module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const p=body(req).password||'';if(!process.env.OWNER_PASSWORD||!process.env.SESSION_SECRET)return json(res,503,{error:'Owner access is not configured'});if(p!==process.env.OWNER_PASSWORD)return json(res,401,{error:'Invalid password'});res.setHeader('Set-Cookie',`lgvc_owner=${encodeURIComponent(makeSession())}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`);json(res,200,{ok:true})};