LA GALERIE DU VIEUX CARRÉ — WEBSITE PACKAGE

Open index.html to preview the site locally.

Files:
- index.html
- styles.css
- script.js
- assets/hero.MOV

IMPORTANT:
Some web hosts/browsers have inconsistent support for .MOV playback. For the most reliable hero video, convert hero.MOV to H.264 MP4 and change the source in index.html to assets/hero.mp4 with type="video/mp4".

The current Reservations link uses reservations@example.com as a placeholder. Replace it with your preferred booking URL or email before publishing.

PROPERTY: 3-bedroom / 3-bathroom private residence in the French Quarter.

DESIGN-LOCKED BOOKING BUILD
---------------------------
The public homepage is based on the approved La Galerie du Vieux Carré design.
No homepage layout, typography, spacing, section order, animation, or visual CSS was redesigned.

Guest booking form: /inquire
Private owner dashboard: /owner

For persistent inquiries and analytics in Vercel, run SUPABASE_SETUP.sql and add:
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
OWNER_PASSWORD
SESSION_SECRET
